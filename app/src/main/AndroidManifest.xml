<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.grocer">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/g0"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".last"></activity>
        <activity android:name=".lastorder" />
        <activity android:name=".l5" />
        <activity android:name=".l4" />
        <activity android:name=".l3" />
        <activity android:name=".l2" />
        <activity android:name=".l1" />
        <activity android:name=".home" />
        <activity android:name=".register" />
        <activity android:name=".login" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>